

const Course = ({course}) => {
    const {title} = course;
    
    return (
        <div className="">
            <h3 className="my-2">title{title}</h3>
            
        </div>
    );
};

export default Course;